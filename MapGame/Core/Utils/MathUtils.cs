using System;
using System.Collections.Generic;
using System.Text;

namespace MapGame.Core.Utils
{
    public static class MathUtils
    {
        public bool InArea(Position pos, AreaList areas);
        public bool isArea(Position pos, Area area);
    }
}
